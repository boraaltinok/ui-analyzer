# Server Configuration
NODE_ENV=development
PORT=3001

# Database
MONGODB_URI=mongodb://localhost:27017/ui-analyzer
# For MongoDB Atlas (recommended for production):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/ui-analyzer

# JWT Secret (Generate a strong secret key)
JWT_SECRET=your-super-secret-jwt-key-here-make-it-long-and-random

# iyzico Configuration
# Get these from your iyzico merchant panel: https://merchant.iyzipay.com/
IYZICO_API_KEY=your-iyzico-api-key
IYZICO_SECRET_KEY=your-iyzico-secret-key

# Frontend URL (for CORS and callbacks)
FRONTEND_URL=https://boraaltinok.github.io/ui-analyzer

# Email Configuration (optional - for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# OpenAI API Key (optional - for built-in AI analysis)
OPENAI_API_KEY=your-openai-api-key-if-providing-built-in-ai

# Webhook Secret (for payment verification)
WEBHOOK_SECRET=your-webhook-secret-key